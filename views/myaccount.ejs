<%- include('./partials/header') %>

    <div class="max-w-2xl mx-auto mt-10 px-6 py-4 bg-white rounded shadow">
        <h1 class="text-2xl font-bold mb-4">My Account</h1>

        <p><strong>Name:</strong>
            <%= user.fullname %>
        </p>
        <p><strong>Email:</strong>
            <%= user.email %>
        </p>

        <p><strong>Registered on:</strong>
            <%= user.createdAt ? user.createdAt.toDateString() : "N/A" %>
        </p>


        <div class="mt-6">
            <a href="/orders" class="text-blue-600 hover:underline">View My Orders</a>
        </div>
    </div>

    <%- include('./partials/footer') %>